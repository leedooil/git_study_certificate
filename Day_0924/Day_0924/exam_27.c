#include<stdio.h>

main() {
	int arr[2][3] = { 1,2,3,4,5,6 };
	int(*p)[3] = arr; //[3]은 행수만 지정해주는 것
	
	printf("%d\n", *(p[0]+1) + *(p[1] + 2)); //2 + 6
	printf("%d\n", *(*(p + 1)+0) + *(*(p + 1) + 1)); // 4 + 5
}